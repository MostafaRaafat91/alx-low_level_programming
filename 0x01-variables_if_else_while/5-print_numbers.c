#include <stdio.h>

/**
 * main - Assign a random n to the variable each time 
 * and print the last digit of the n
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
